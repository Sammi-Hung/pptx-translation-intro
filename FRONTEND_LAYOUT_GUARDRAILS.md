# 前端版面守則

這個專案的前端首頁版面已由使用者確認，後續修功能時需遵守以下規則，避免視覺漂移。

## 固定結構

- 第一列維持兩欄：
  - 左欄：大型 PowerPoint 上傳區。
  - 右欄：語言、翻譯模型、Windows 備註語音包狀態與開始按鈕。
- 下方維持狀態區：
  - 處理中進度卡。
  - 完成結果卡。
  - 錯誤卡。
- 頁面底部維持兩個淡色說明區塊。

## 新功能嵌入規則

- 不新增新的大型卡片或新的頁面區塊，除非使用者明確要求。
- 新設定優先放入右側既有「語言與語音」區塊。
- 新操作優先放入既有進度卡或既有按鈕列。
- 不新增新的主色系、漸層、外框卡片或裝飾元素。
- 新欄位需沿用現有 select/input/button 的尺寸與 Tailwind class。

## 修改前檢查

- 若只是修後端、API、模型、語音或檔案處理，不應修改 `frontend/app/page.tsx` 的主 grid 結構。
- 若必須改 UI，先確認是否能改在既有元件內。
- 修改後需執行 `pnpm build`。

## 服務狀態顯示

- 模型設定與語音包狀態是後端健康度訊號，不應因單次 `Failed to fetch` 永久停留錯誤。
- 前端需自動重試模型設定與語音包狀態。
- 重試訊息只能放在既有欄位或既有語音狀態框，不新增版面區塊。
